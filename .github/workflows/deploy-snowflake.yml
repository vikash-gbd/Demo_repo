name: 🚀 Deploy to Snowflake via Liquibase

on:
  push:
    branches: [main, master]

jobs:
  deploy:
    runs-on: ubuntu-latest

    env:
      SNOWFLAKE_ACCOUNT: ${{ secrets.SNOWFLAKE_ACCOUNT }}
      SNOWFLAKE_USERNAME: ${{ secrets.SNOWFLAKE_USERNAME }}
      SNOWFLAKE_PASSWORD: ${{ secrets.SNOWFLAKE_PASSWORD }}
      SNOWFLAKE_ROLE: ${{ secrets.SNOWFLAKE_ROLE }}
      SNOWFLAKE_WAREHOUSE: ${{ secrets.SNOWFLAKE_WAREHOUSE }}

    steps:
      - name: 📥 Checkout repo
        uses: actions/checkout@v4

      - name: ☕ Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: 📦 Download Liquibase CLI
        run: |
          curl -sLO https://github.com/liquibase/liquibase/releases/download/v4.24.0/liquibase-4.24.0.tar.gz
          tar -xzf liquibase-4.24.0.tar.gz

      - name: 🔧 Replace placeholders in liquibase.properties
        run: |
          esc() { printf '%s' "$1" | sed -e 's/[\/&]/\\&/g'; }

          sed -i "s/__SNOWFLAKE_ACCOUNT__/$(esc "$SNOWFLAKE_ACCOUNT")/g" liquibase.properties
          sed -i "s/__SNOWFLAKE_WAREHOUSE__/$(esc "$SNOWFLAKE_WAREHOUSE")/g" liquibase.properties
          sed -i "s/__SNOWFLAKE_ROLE__/$(esc "$SNOWFLAKE_ROLE")/g" liquibase.properties
          sed -i "s/__SNOWFLAKE_USERNAME__/$(esc "$SNOWFLAKE_USERNAME")/g" liquibase.properties
          sed -i "s/__SNOWFLAKE_PASSWORD__/$(esc "$SNOWFLAKE_PASSWORD")/g" liquibase.properties

      - name: 🧪 Run Liquibase updateSql
        run: |
          ./liquibase-4.24.0/liquibase \
            --defaultsFile=liquibase.properties \
            updateSql

      - name: ✅ Mark as executed (changelogSync)
        run: |
          ./liquibase-4.24.0/liquibase \
            --defaultsFile=liquibase.properties \
            changelogSync
